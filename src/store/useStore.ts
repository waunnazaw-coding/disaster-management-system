// import {create} from 'zustand';
// import produce from 'immer';
// const initialState = {
//     currentUser: null,
//     language: 'en', // Default language
// };
// export const useStore = create((set) => ({
//     ...initialState,
//     setUser: (user ) => set(produce((state) => { state.currentUser = user })),
//     setLanguage: (lang) => set(produce((state) => { state.language = lang })),
// }));