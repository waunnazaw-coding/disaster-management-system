// This file acts as a simple global state for dashboard statistics.
// In a more complex application, you might use a dedicated global store
// or a server-side cache for these values.

export const dashboardStats = {
  pendingReports: 0,
  pendingRequests: 0,
  pendingDonations: 0,
  unassignedRequests: 0,
  approvedRequests: 0,
  totalRequests: 0,
  // Add other stats as needed
}
